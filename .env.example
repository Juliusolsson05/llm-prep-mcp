# LLM Context Prep MCP Server - Environment Configuration

# Debug mode (true/false)
MCP_DEBUG=false

# Maximum file size in bytes (10MB default)
MCP_MAX_FILE_SIZE=10485760

# Maximum context document size in bytes (50MB default)
MCP_MAX_CONTEXT_SIZE=52428800

# Allowed file extensions (comma-separated)
MCP_ALLOWED_EXTENSIONS=.py,.js,.ts,.jsx,.tsx,.java,.c,.cpp,.cs,.go,.rs,.rb,.php,.swift,.kt,.scala,.r,.m,.h,.hpp,.sh,.bash,.zsh,.fish,.md,.txt,.json,.yaml,.yml,.toml,.xml,.html,.css,.scss,.sass,.less,.sql,.graphql,.proto,.dockerfile,.makefile,.cmake,.gradle,.maven

# Docker mode (set automatically in Docker)
# MCP_DOCKER_MODE=true

# Workspace directory (for Docker)
# Windows users: Set this to your absolute project path like C:\Users\you\repo
WORKSPACE_DIR=C:\path\to\your\project
MCP_WORKSPACE_DIR=/workspace

# Optional: Transport mode (stdio, sse, http)
# MCP_TRANSPORT=stdio

# Optional: Server port (for HTTP/SSE mode)
# MCP_SERVER_PORT=8080

# Optional: Server host (for HTTP/SSE mode)
# MCP_SERVER_HOST=0.0.0.0
